#include <stdio.h>

float square(float num) {
    return num * num;
}

int main() {
    float num, result;
    
    printf("Input any number for square : ");
    scanf("%f", &num);
    
    result = square(num);
    
    printf("The square of %.2f is : %.2f\n", num, result);
    
    return 0;
}
